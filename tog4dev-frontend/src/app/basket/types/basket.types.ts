export type BasketItem = {
  id: number,
  category: string,  
  quantity: number,
  country: string,
  description: string,
  location: string,
  totalPrice: number,
  thumbnail: string,
  title: string,
};