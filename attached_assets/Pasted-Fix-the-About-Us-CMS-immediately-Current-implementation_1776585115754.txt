Fix the About Us CMS immediately. Current implementation has critical broken functionality.

====================================================
URGENT BUG FIXES
================

1. ARABIC VERSION DOES NOT WORK
2. Publish / Unpublish buttons do nothing
3. Save data in sections does not work
4. Frontend published page renders broken / blank sections
5. Preview button returns raw JSON instead of real page
6. Remove preview button for now

====================================================
PART 1 — FIX ARABIC VERSION
===========================

Current issue:
Arabic page not loading / not switching / not rendering.

Fix fully:

* language switch must support ar + en
* load Arabic content correctly from DB
* RTL layout when locale = ar
* Arabic text fields save/load properly (UTF-8 safe)
* fallback to English only if Arabic missing

Admin panel:

* editing Arabic content must save correctly
* language selector must work

====================================================
PART 2 — FIX SAVE BUTTON / SECTION DATA SAVE
============================================

Current issue:
Saving section content does nothing.

Fix:

* all forms submit correctly
* API update endpoints work
* validation errors shown
* success toast/message shown
* data persists in DB
* reload page shows saved content

Check all sections:

hero
intro
highlights
statement
visionMission
coreValues
founders
beliefs
stats
slogan
contact
partners
SEO

====================================================
PART 3 — FIX PUBLISH / UNPUBLISH
================================

Current issue:
Buttons do nothing.

Fix logic:

Publish:

* mark current draft as published
* frontend uses published version only
* success message shown

Unpublish:

* remove live published state
* page hidden or fallback safely

Buttons must visually reflect state:

Draft / Published / Unpublished

====================================================
PART 4 — FRONTEND PAGE BROKEN AFTER PUBLISH
===========================================

Current issue:
Published content shows empty broken layout.

Fix:

* frontend must consume API safely
* no blank white blocks
* no broken spacing
* hide empty sections
* render only sections with content
* fallback to global if country missing

If hero missing:
hide hero cleanly

If section empty:
do not render placeholder block

====================================================
PART 5 — REMOVE PREVIEW BUTTON
==============================

Current preview returns JSON endpoint.

For now:

* remove Preview button from admin UI completely
* remove related broken link

Do not show unusable features.

====================================================
PART 6 — ADMIN UI POLISH
========================

Improve About Us CMS panel:

* save buttons visible
* publish button highlighted
* loading states
* disabled while saving
* error messages clear
* responsive layout

====================================================
PART 7 — DATABASE / API CHECK
=============================

Audit and fix:

* locale field
* country field
* status field
* published_at
* updated_at
* versioning consistency

Ensure APIs return proper JSON and mutations persist.

====================================================
PART 8 — FINAL QA TEST
======================

Must verify:

1. Save English content works
2. Save Arabic content works
3. Switch languages works
4. Publish works
5. Unpublish works
6. Frontend page renders real content
7. No blank broken sections
8. Admin reload keeps data

====================================================
DELIVERABLE
===========

Fix directly in project and show:

1. files changed
2. root causes found
3. what was fixed
4. how publish flow works now
