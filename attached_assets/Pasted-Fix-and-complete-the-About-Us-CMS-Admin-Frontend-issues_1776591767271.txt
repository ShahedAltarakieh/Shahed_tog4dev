Fix and complete the **About Us CMS Admin + Frontend** issues below. Please implement all items carefully and make everything production-ready.

# ABOUT US CMS – Required Fixes

## 1. Admin Page Header Layout

The About Us CMS admin page top area looks broken.

### Fix it like other admin pages:

Use same structure as other pages:

* Back button on left
* Page title beside it
* Breadcrumb under title

Example:

Dashboard / Content / About Us

Use consistent spacing, typography, alignment.

---

## 2. Language File Fix

Currently showing:

app.create About Us CMS

### Fix:

Move labels into lang files.

Use translation key:

about_us

NOT:

about us cms
NOT app.create

Need proper translations:

### EN

* About Us
* Create About Us
* Edit About Us

### AR

* من نحن
* إنشاء صفحة من نحن
* تعديل صفحة من نحن

Also breadcrumb must use translations.

---

## 3. Frontend Header Must Match Other Pages

About Us frontend page header is different/broken.

Make it match styling of:

* News & Gallery page
* Contact page
* Other static pages

Use same:

* hero top spacing
* overlay
* title alignment
* responsive spacing
* typography

---

## 4. Remove OG Image Field

No need for:

OG Image

Remove from admin UI + backend validation + save logic if unused.

Keep code clean.

---

## 5. Spacing Between Icons and Labels

In admin CMS card:

Need spacing between icon and text.

Example:

[icon] Sections

not glued together.

---

## 6. Publish / Unpublish Still Not Working

Buttons still broken.

Fix fully:

### Publish:

* save current draft
* mark published
* visible on frontend

### Unpublish:

* remove live version
* optionally keep draft

Show success toast.

No page refresh required.

---

## 7. Drag to Reorder Not Working

Section reorder still broken.

Implement working drag & drop.

Requirements:

* drag handle visible
* smooth dragging
* save new order to DB
* persists after refresh
* frontend respects order

Use sortable solution already used in project if available.

---

## 8. Save Section Data Not Working

Each section Save button currently fails.

Fix all section save logic:

* validate fields
* save section content only
* toast success
* keep page state
* no reload required

Need to work for ALL sections.

---

## 9. Hero Section Simplification

Hero section currently expects image/media.

No need image for hero.

Hero should only support:

* Subtitle AR / EN

Remove:

all other filed 

Frontend hero uses styled background like other pages.

---

## 10. New Admin Page: Navigation Visibility Manager

Create new admin page to control which frontend menu items appear in navbar.

Need toggle visibility for:

* News & Gallery
* About Us

Future scalable for more pages.

### Requirements:

Fields:

* page_key
* visible
* order (optional)

### Frontend:

Navbar should load from API / config.

If disabled:

hide menu item completely.

No hardcoded menu visibility.

---

# Additional Requirements

## Backend

* fix routes
* fix controller save methods
* fix ajax responses
* proper validation

## Frontend

* dynamic rendering only
* no hardcoded menu items
* responsive

## UX

* success/error toast messages
* loading states on save/publish

## Final QA

Must verify:

* save section works
* publish works
* unpublish works
* reorder works
* navbar toggles work
* translations work
* frontend About Us matches site design

Do not leave placeholders. Do full implementation.
